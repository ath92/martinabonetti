export default class PortfolioItemData {
	//ID: int
	//Title: string
	//Featured image: image
	//Icon: image
	//Related items: array with related items (max 2, however contentful handles that)
	//Projects: array with project references (however contentful handles that)
}